Publish the complete Sciences of Language V2 source-local pattern evidence into GitHub under `Sciences_of_Language_V2/Per_Source/<CORPUS>/Patterns/` using the current-scanner artifacts from run `32224574035`. Require exactly 14 corpus audits and exactly 84 complete pattern tables (6 kinds × 14 sources), gzip integrity checks, SHA-256 hashes, exact row counts, and Git-safe file sizes. Re-aggregate V2 from those source-preserving artifacts before publication so the report/manifests match the current scanner. Do not merge if any expected table is absent.

Retry after artifact-identity reconstruction fix: 2026-08-19.